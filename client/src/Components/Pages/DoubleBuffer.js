import React from 'react'

import Template from '../PageTemplate'

export default () => {
    return (
        <Template title='Double Buffer'>
            <h2>What is Double Buffer?</h2>
            <p>
                Double Buffering is a term used to describe a device that has two buffers. The usage of multiple buffers increases the overall throughput of a device and helps prevent bottlenecks.    
            </p>
        </Template>
    )
}
